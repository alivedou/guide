# 项目需求文档 (Requirements Document) - CloudNav

## 1. 项目概述
CloudNav 是一个基于 Cloudflare 生态系统（Pages + Workers KV + D1）构建的高度自定义、高颜值的多用户个人导航网站。本项目利用 KV 实现快速缓存与配置存储，D1 实现稳固的结构化用户数据持久化。旨在为用户提供一个极致简洁、响应迅速且易于管理的个性化门户。

## 2. 核心目标
- **极致简洁与沉浸交互**: 支持一键智能切换为“禅意/沉浸桌面”，大幅消除视觉噪音，实现信息获取的“零负载”。
- **单视图隔离检索**: 将传统长滚动列表与全新的“单分类隔离渲染”深度融合，随心在“长页纵览”与“单卡片沉浸”中自由穿梭。
- **全键盘导向操作**: 业内一流水准的全键盘导航体验，无需鼠标即可完成跳转、检索、偏好切换及多用户管理。
- **高效检索**: 集成多引擎搜索引擎，提供模糊匹配、站内直达和一键跳转。
- **云端存储**: 利用 Cloudflare KV 实现无服务器架构的持久化存储，无需维护传统数据库。

## 3. 技术栈
- **前端**: 原生 HTML5, CSS3 (Tailwind CSS 思想), Vanilla JavaScript
- **后端**: Cloudflare Pages Functions (Serverless)
- **存储**: Cloudflare Workers KV (缓存与配置) + Cloudflare D1 (关系型数据持久化)
- **编辑器**: Monaco Editor (用于在线修改 JSON 配置)

### 3.1 后端 API 架构设计 (文件夹结构)

建议采用基于 Cloudflare Pages Functions 的 `/functions/api` 结构，实现模块化 API 管理：

/functions/
└── api/
    ├── auth/                 # 用户身份认证相关
    │   ├── login.js
    │   ├── register.js
    │   ├── logout.js
    │   └── check.js          # 会话验证
    ├── admin/                # 管理员/特权用户操作
    │   ├── users.js          # 用户管理 (列表/修改/冻结)
    │   ├── config.js         # 全局系统配置管理
    │   └── announcements.js  # 公告发布与管理
    └── user/                 # 普通用户操作
        ├── profile.js        # 个人资料修改
        └── data.js           # 页面导航数据 persistence

## 4. 功能列表

### 4.1 核心搜索与导航模式
- **双模导航形态**:
    - **极简沉浸模式 (Zen Mode)**: 启动时屏幕正中央仅显示唯一的聚焦搜索框，侧边栏全设备强制隐藏。任何交互触发（鼠标点击/较大范围移动/敲击 Enter）后将平滑切入并向下展开含有“常去网站”项(如有开启)与导航主菜单的“导航视界”。进入导航视界后，默认仅显示第一个分类下的全部网址窗格。后续每次切换分类时，也严格遵循单一视图原则，仅单独展示所选定分类项下的全部网址窗格，屏蔽其他分类内容干扰。桌面端使用 `Ctrl/Cmd + B`快捷键进行视图调度；移动端或平板设备额外增加触控界面的左向右拉拽式抽屉划手势触发调度面板。
    - **标准模式 (Standard Mode)**: 摒弃了过渡式的首屏隐藏机制，始终直接挂载并常驻全视角视图下所有分类长廊导航架构。
- **常去网站智能生成机制 (Smart Frequent Sites)**:
    - **动态判定策略**: 依靠记录用户最近一周内的点击并跳转行为数据，当某一资源链接在近 7 日内累计被点击流转达 10 次及以上时，系统自动将其判定并纳入“常去网站”前端列表展现中。
    - **系统全托管特性**: 常去网站版块采取绝对系统根据数据动态生成的自动归集推演模式，不支持且阻断任何人工手段在面板上执行固定强加新增、或是强行锁定绑定的干预操作（不可人为手动创建与置顶）。
- **检索联动与入口**:
    - **键入即唤醒**: 在未聚焦或非静止极简态背景下，键击敲出任何字母符等操作均会产生触发级全屏搜索唤起反应、强制聚焦至此唯一可用级入口内。
    - **智能联想捕捉**: 跟随连续动态地键入输入序列同步向下推发展开联想分派下拉辅助菜单，完全放开对接全原生方向键焦点流转掌控能力。
    - **本地搜推优先过滤算法机制**: 搜罗并优先进行针对被挂靠在本系统架构的资源池中数据的配比；凡高优先级且存在确切对应的局部资产直接展现快捷抵达直卡，而判定完全无对应后系统动态进行视图回缩收拢行为，输入核心变为向外部主寻查引器提交请求的传统输入容器用器机制。
- **快捷键与全键盘控制 (Keyboard Navigation & Shortcuts)**:
    - **Esc 一键复位**: 高优先级响应。任意视图下按 Esc 键可立即关闭所有悬浮窗体、弹窗与侧边栏，退回至最基础的搜索输入状态。
    - **搜索历史回溯**: 在搜索框为空时，支持使用键盘上下方向键（ArrowUp/ArrowDown）快速翻卷与回显本地搜索历史记录。
    - **Ctrl/Cmd + K 快速聚焦**: 全局快捷键，按下后立即清空搜索框、接管焦点并自动聚焦到搜索框。
- **多引擎无缝切换**: 搜索框前端支持切换不同的搜索引擎（Bing, Google, Baidu, Sogou），并支持引擎选择状态的持久化记忆（保存在 LocalStorage 或 KV 中），确保刷新或下次访问时维持上次的偏好。

### 4.2 视觉体系与底层交互
- **视觉排布与界面形态**: 
    - **主题光彩模式调度控制**: 调控设定器统一沉降绑定侧边最适配手感下沉位及关联映射界面内端，给予明确的“系统级响应匹配跟随/纯黑重色化/白光清爽化”三段状态首尾贯连反复切调权限。
    - **侧边栏挂靠样式装配策略**: 同时保留两套极具差距比美观界态支持热加载交替方案：即强调弱光干扰强结构框体的经典超清爽形制以及用对比显色区隔带加注醒目图形强标重点信息的彩色拟物向设计形态供用户端定。对于广体宽幅界支持全固定图钉效果扎堆或挂浮展示并根据紧缩受限的竖频便携环境智能转换抽拉藏身遮盖行为操作体系。
    - **宽容动态栅格排列部署规则**: 统括整个图阵网排位密度间隔，授予三维间距收纳度：兼顾屏幕内容装载极限最大紧凑配置项（紧凑）、通用平衡体验基点配置（标准）到强透气闲适放松排列要求展现支持档参数（舒适档位布局）。
- **全效容灾补漏自愈标配支持**: 特配置对于在网络抖动或远端灭失引发的载图不全缺漏情况，搭建从首级加载失败至自动转拉取自建备用数据镜像池再到最后退至全屏以特定生成文字带色纯快卡片式兜底掩护策略流程体系。同时单独预配支持超级管控的管理员手动唤醒实施魔法棒重查刷新救护按键权限支持接口。
- **长页面滚动辅助 (Scroll Management)**:
    - **悬浮操作按钮 (To-Top/Bottom FAB)**: 仅在页面向下滚动超过一定阈值时显示，提供“一键回顶”和“一键至底”按钮。按钮点击区域不少于 44px（符合无障碍触控规范），处于静止状态时自动半透明或隐藏以减少视觉干扰。
    - **全局滚动快捷键**: 捕获 `Home` 键平滑滚动至页面最顶端，`End` 键平滑滚动至最底端，支持 `PageUp` / `PageDown` 和 `Space` 进行翻页。
- **无障碍与焦点管理 (Accessibility & Focus)**:
    - **全场景焦点流转**: 所有交互界面（如主导航、设置面板、弹窗、输入表单等）的所有可交互元素（如按钮、链接、选项框）必须支持通过 `Tab`（正向）和 `Shift + Tab`（逆向）进行焦点切换，并有清晰的视觉聚焦框（Focus Ring）。
    - **键盘激活**: 获得焦点的元素在按下 `Enter` 或 `Space` 键时，必须 100% 触发等效于鼠标点击的交互动作。
- **UI 状态重置与层级规范 (UI State Reset & Layers)**:
    - **状态自动清理 (State Cleanup)**: 当触发重置、登出、关闭分类、或关闭任意弹窗/抽屉/下拉菜单时，其关联的临时状态（如未保存的输入内容、表单草稿、悬浮框、次级聚焦状态等）必须即时清空，防止出现任何“鬼影状态残留”。
    - **层级规范 (z-index Layout)**: 对全局 UI 叠放层级（z-index）进行严格的层级规划。确认身份、管理面板等关键全局悬浮窗口必须拥有最高的层级资源，避免被底部普通页面元素遮挡或穿透。
- **管理员操作高亮提示 (Admin Active Banner)**:
    当管理员 (Admin) 或特权用户 (Super User) 正在执行全局系统配置修改、或干预他人资料等敏感/特权操作时，页面顶部必须常驻展示一个明显的警告性悬挂宣告栏。在特权操作结束或退出管理模式后，该宣告栏随之自动消失并恢复正常视图，防止误操作。

### 4.3 账号与系统
- **鉴权登录**: 基于 Token 机制进行安全会话验证，支持多级别用户角色权限隔离。
- **用户角色与权限体系**:
    - **游客 (Guest)**: 未登录状态下的默认用户。仅能浏览系统公共视图（由管理员配置的统一默认导航页面），无任何管理或修改权限。
    - **登录用户 (User)**: 已注册并登录的正式用户。拥有专属 UUID，享有独立的数据存储隔离（D1/KV），可完全自定义个人的导航内容、主题、偏好设置等。
    - **特权用户 (Super User)**: 由管理员授予特权标签。专注日常运营与维护，权限包含：全站用户查询、账号状态【冻结/解冻】、敏感禁词维护、在指定配额限制下生成邀请码。
    - **管理员 (Admin)**: 拥有系统最高管控权限。除享有特权用户的全部权限外，独享以下核心权限：全局系统配置修改（注册/邀请策略、超管配额、风控安全、语言、时区等）、执行高危账号彻底清理/删除、查看全量系统审计日志、全局公告发布与展示策略调整。
- **系统配置菜单 (System Configuration)**: [仅限管理员 (Admin)]
    - **参数概览**: 集中管理系统运行参数，支持数值设置与功能开关。
    - **配置项**:
        - **全局站头元数据配置 (Site Metadata)**: 支持管理员灵活设定全站全局基础 SEO 与品牌标识属性，包含但不限于全局 `Title`（网站标题）、主 `Favicon` 图标、全局 `Logo` 以及页面的 `Keywords`（关键词）与 `Description`（站点描述），支持从前端管理控制面板直接下发落库并实时生效。
        - **注册与邀请策略**: 仅限【管理员 (Admin)】操作。支持全局开启/关闭【开放注册】与【邀请码注册】独立开关，决定系统访问策略。
        - **超管配额管理**: 设定“超级用户”单次/每日生成邀请码的数量上限。
        - **账号风控与安全策略 (Security Policy)**: 增加密码防爆破锁定判定机制（例如同一账号连续密码错误达到阈值则实行设备或时间维度的冻结锁定）；同时提供会话时效（Token 存活期）维持与超期切断策略配置，深度保护个人数据安全。
        - **全局公告策略**: 设置系统公告默认展示等级。
        - **时区设置**: 设置系统全局运行时区，影响所有时间类内容的显示。默认以北京时间 (UTC+8) 为准。
        - **语言策略**: 设置系统默认展示语言，并选择启用支持的语言包（如中/英），管理员可控制前端用户可见的语言选择范围。默认提供中文与英文支持。
    - **生效机制**: 采用数据库/KV 实时存储，管理员保存配置后页面无需刷新即可全局生效。
- **公告管理菜单 (Announcement Management)**:
    - **公告概览**: 展示已发布、定时发布、草稿等状态的公告列表，支持标题搜索与创建人筛选。
    - **内容创作**: 支持富文本编辑、发布状态管理 (草稿/发布/归档)、定时推送设置、支持公告置顶与公告弹出权限设置。
    - **用户端展示策略 (User Display)**:
        - **展示层级**: 采用非模态 (Non-blocking) 设计。
        - **静默/重要模式**: 支持【静默模式】(右下角弹窗并在 5 秒后自动向右上角缩回为小铃铛通知图标，实现提示无感) 与【重要通知】(在屏幕顶部以极薄的浅色条 banner 形式持续悬浮，直到用户主动关闭)。
        - **交互要求**: 确保所有弹窗不遮挡核心导航区域，支持 ESC 快捷键立即关闭，兼顾通知到达率与极简使用体验。
    - **权限控制**:
        - **管理员 (Admin)**: 全局管理权限，包括发布、修改、归档、彻底删除所有公告、以及系统级公告展示策略调整。
        - **超级用户 (Super User)**: 内容协同权限，支持创建/编辑公告内容，仅能管理自身创建的公告，无法删除已归档的历史数据或重置系统级公告配置。
- **用户管理菜单 (User Management)**:
    - **权责隔离**: 用户管理操作严格遵循 **[用户角色与权限体系]** 的定义。特权用户 (Super User) 仅可执行日常运营，管理员 (Admin) 独享高危管理、全局注册配置及全量审计等核心权限。
    - **用户概览 (Dashboard)**: 全站用户列表展示，设计重点为【快速检索与即时操作】。支持关键字搜索、状态筛选，列表支持【多选操作模式】、分页控制 (支持自定义每页展示条目数: 20/50/100 条)、【CSV 批量导出】及折叠式搜索/筛选面板，在无需检索时可折叠隐藏以节省界面空间。每行支持点击进入“账号配置”，或进行高频单行操作。
    - **账号配置 (Detail Management)**: 针对特定用户的【细颗粒度配置中心】。用于执行复杂特殊设置，如账号状态微调、重置邀请关联、【角色权限调整】以及【密码重置功能】。执行高危操作时，系统强制要求管理员/特权用户【二次输入登录密码】验证身份。
    - **账号冻结机制 (Account Freeze)**:
        - **即时阻断**: 一旦执行冻结，系统立即吊销用户的所有当前活跃会话，禁止任何新的登录尝试。
        - **数据安全**: 冻结仅为访问限制，不删除任何用户数据或个人配置，保留归档状态供解冻恢复。
        - **提示反馈**: 冻结用户尝试访问时，系统返回“账号已被限制访问，请联系管理员”的明确提示。
    - **禁用词库 (Banned Keywords)**: 集中维护用户名审核的敏感词库，支持批量操作与测试。
    - **邀请码管理**: 集成注册邀请码生成与管理功能，支持【自定义邀请码内容】、批量生成及状态监控。
        - **单码单用原则**: 每个生成的邀请码均为独立唯一。
        - **生成权限**: 超级用户受限批量生成（基于配额），管理员无限制。
    - **审计日志 (Audit Log)**: 记录所有敏感管理操作，支持多维度检索与批量导出，超级用户仅可只读，管理员可彻底清除。
- **账号识别机制**: 系统采用全局唯一、不可变的 ID (UUID) 作为内部统一凭证；用户名可变更且需满足敏感词和防碰撞校验。
- **用户个人资料管理**:
    - **登录用户个性化界面**: 支持切换内置头像库、调整语言偏好、基于配额修改用户名（系统强制服务器端敏感词过滤校验），并直观显示身份标签 Badge 与 UUID。
    - **未登录用户状态**: 显示空白默认头像与 “无名” 标识，不支持个人资料交互。

### 4.4 页面管理 (Page Management)
- **全局基础定义**: 系统内置一个只读的 `system_default.json` 文件，作为全系统的“出厂设置”源。
- **内容 CRUD 机制**: 提供对分类、以及分类下挂载的网址卡片的精细化增删改查支持（特色支持视频特殊卡片挂载与 Bilibili 自动封封面获取）。
- **管理菜单内容**: 进入页面管理后，弹出的子菜单包含以下核心选项：
    - **内容偏好设置 (Content Preferences)**: 
        - **网格尺寸**: 紧凑 (85px), 标准 (105px), 舒适 (120px) 模式切换。
        - **极简沉浸模式**: 设置极简沉浸模式开关。
        - **常去网站栏**: 设置是否展示常去网站模块。
        - **自定义背景**: 自动拉取 Bing 每日高清壁纸（带有半透明遮罩与 24 小时缓存），支持覆盖为自定义图片 URL 或纯色。
        - **简约模式 (背景模糊)**: 控制背景的高斯模糊效果。
        - **窗口打开方式**: 控制链接是“直接跳转”还是“另开一页”。
        - **分类管理与排序**: 列表式呈现侧边栏分类，支持拖拽排序、自定义图标/Emoji、命名、视频模式切换及可见性控制。
    - **JSON 编辑 (JSON Edit)**: 内置富文本 JSON 编辑器，支持配置的底层高级修改，且与前端 UI 实施双向绑定实时预览；支持自动美化格式。
    - **保存 (Save)**: 将所有更改（内容偏好、JSON 及组件的改动）即时持久化写入 KV/D1 数据库。
    - **导出/导入 (Export/Import)**: 导出当前个性化 JSON 配置 (如 `[ID]_[月][日].json`)，并支持上传合法格式的配置并一键覆盖。**（注：本网页强定制系统的专属导入格式底层结构锁定不可更改。面对日常多见浏览器书签导入需求，系统将在功能弹出项处与项目开源 README 文件中友情提供通用的第三方格式在线转换辅助跨界桥梁工具链接，强烈引导使用者务必自行采用它界外辅助工具完成书签链结至系统原生 JSON 标准形态的翻译转换加工手续后且复行系统本身的标准引入流转）。**
    - **恢复默认 (Reset)**: 强制将当前视图配置基于预设或 `system_default.json` 状态回滚。
    - **退出 (Exit)**: 关闭管理抽屉/视口并清理操作状态。
- **页面内容可视化与批量管理**:
    - **页面管理 (Page Management) 模式**: 专为高频维护场景设计的管理套件。开启后，系统进入“非线性编辑”状态：
        - **自由排版**: 支持磁贴在不同分类间的拖拽迁移（Drag & Drop），基于 SortableJS 实现丝滑的跨分类重排。
        - **批处理模式**: 在管理状态下，可通过单击勾选多个网址窗格，弹出底部批处理栏，执行【移动至指定分类】或【一键批量删除】操作。
    - **新增入口**: 分类末项自动挂载“+ 新增”虚线卡片，用于快捷新建。
    - **网址窗格详情编辑**: 
        - 提供标题、URL、描述修改。
        - **魔法棒 (Smart Fetch)**: 一键触发多源图标智能获取（根目录 favicon -> 配置图标库 -> 首字母高亮替代）。
        - 图标定制支持输入 URL、选择预置库，或调用智能 Emoji 生成。
- **数据同步安全性**: 所有的拖拽与批量变更均通过防抖（Debounce）机制自动同步至 Cloudflare KV/D1，确保操作的高效与数据一致性。
- **页面管理权限层级**:
    - **个人页面管理 (Personal Page Management)**: 所有登录用户均可访问自己的页面管理面板，并实时应用至自身账号。
    - **游客页面管理 (Guest Page Management)**: [仅管理员 (Admin)] 提供独立控制面板配置游客访问默认展示的内容布局与基础偏好。

### 4.5 跨端互融统配与视觉交互同态一致化网络构建 (Cross-platform & Visual Consistency)
- **PWA 渐进式离线支持机制 (PWA Progressive Web App 支持)**: 前端构建完全接轨标准 PWA 服务底层规范注册能力，准允所有终端用户横跨各品牌操作宿主设备直接一触生成创建沉浸态独立的启动入口图标驻留主桌，享有不输独立客户端级别的开屏与运行体验。
- **实时交互反馈**: 所有可交互的 UI 元素（按钮、菜单项等）必须提供即时、明确的状态反馈（如悬浮态、点击动画、加载中指示），避免因无反馈导致的重复点击。
- **全主题 UI 一致性**: 保证所有交互页面、文字内容、弹窗及图标在“亮色模式” (Light Mode) 与“暗色模式” (Dark Mode) 下均具备优异的视觉对比度与清晰度，严格遵循 A+ 级辅助功能配色设计，确保视觉友好且阅读负担最低。

### 4.6 多用户数据架构与资源配额 (Multi-user Architecture & Quotas)
- **数据隔离与多租户 (Data Isolation)**:
    - **D1 持久化层**: 所有用户生成的数据均需通过强关联的 UUID (User ID) 进行严格的租户隔离，防止越权访问 (IDOR)。
    - **KV 缓存策略**: 设计 `user_config:{uuid}` 键结构进行缓存。读操作优先命中 KV 以实现低延迟响应；写操作（保存修改）先落盘 D1，随后自动驱逐或更新对应的 KV 缓存，确保数据最终一致性。
- **资源配额限制 (Resource Quotas)**: 
    为了防止存储滥用及保障系统性能，实施以下单用户数据硬性约束：
    - **分类数量上限**: 每个用户最多可创建 **20** 个分类。
    - **单分类网址上限**: 每个分类下最多可添加 **50** 个基础网址卡片。
    - **前端阻断机制**: 当达到上限时，前端 UI 将自动禁用“新增”/“添加分类”按钮，并给出清晰的高亮提示（如“分类数已达上限，请整理后添加”）。
- **新用户初始化 (Data Initialization)**:
    - 新用户首次注册并登录时，系统自动拦截空状态，将其基础数据通过 `system_default.json` 或管理员配置的“统一公共视图”进行全量拷贝，确保新手具备「开箱即用」的页面结构。
- **并发与防刷防护 (Anti-Abuse & Rate Limiting)**:
    - 基于 Cloudflare Worker 的运行时特性，在 API 鉴权层引入简易的防刷机制：对用户的高频写操作（如高频拖拽排序保存、高频新增配置）加以防抖（Debounce）及基础的 IP/账号频率速率限制（Rate Limit，如限制 `/api/admin/save` 的请求频率），保障 D1 数据库资源。

## 5. 项目状态
### 最近更新
1. **全键盘导航系统**: 实现了全局的 ArrowKey 几何焦点导航，Modal 焦点陷阱（确保 Escape 和 Tab 安全退出），Enter/Space 点击激活。
2. **搜索栏与常去网站智能可见性**: 默认隐藏，在输入时智能调出，Esc 键自动隐藏，优化纯净度。
3. **侧边栏样式合并**: 将经典模式与缤纷模式的样式切换按钮合并为单按钮循环切换，精简界面。
4. **侧边栏子菜单导航优化**: 支持使用上下方向键在侧边栏子分类/按钮间进行循环切换。
5. **重构 requirements 文档结构**: 按照新的规范要求对项目需求文档进行了模块化梳理，以提高可维护性。

---
