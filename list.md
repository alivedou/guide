# CloudNav 完善阶段待办事项 (Combat Map) - V4

## 待修复缺陷 (UI Bugs)
- [x] **缺陷 1**：中央搜索框输入英文字母 `j` 时下延笔画被边缘裁切，导致其辨识为 `i`。 (已修复)
  - *改进点*：为 `#sea-input` 添加上下内边距 `padding: 4px 0 !important`，并设定舒适的 `line-height: 1.4`，扩充垂直渲染边界。
- [x] **缺陷 2**：PC端唤醒搜索框时，由于主内容区域（`.main-content`）带有 transition/will-change 属性，导致 fixed 定位的 `.search-section` 偏移，左侧边栏区域露出未被遮罩覆盖。 (已修复)
  - *改进点*：将 `.search-section` 的 DOM 结构从 `index.html` 中的 `.main-content` 内部移出，提升为平级节点，使其参照浏览器根视区全屏覆盖。
- [x] **缺陷 3**：开启背景模糊后，叠加上书签卡片或分类标题自身的背景模糊，导致嵌套毛玻璃渲染冲突，书签名称与分类文字边缘发虚、字迹模糊。 (已修复)
  - *改进点*：当背景模糊开启时，自动利用 CSS 层级禁用卡片及分类标题的多重 `backdrop-filter`，释放 subpixel 抗锯齿渲染；同时为卡片文本增加微弱阴影和 GPU 字体锐化样式。
- [x] **缺陷 4**：在页面管理中批量隐藏或编辑隐藏书签后，退出管理模式时漏掉了自动云端同步逻辑，导致修改仅存在于本地缓存，他人访问分享的主页时依然能看到所有隐藏的书签。 (已修复)
  - *改进点*：在 `handleDataSaveOnExit()` 中补齐登录态自动同步逻辑，如果用户已登录并开启了自动同步，则退出管理模式时立刻静默调用 `manualSyncCloud(false)` 进行云端更新，确保数据 100% 同步到 D1/KV。同时对“常取网站”分类添加 `hidden` 属性的渲染强校验。
- [x] **缺陷 5**：进入页面管理后，由于常规书签卡片高度由内容决定（约105px），而“新增卡片”与“骨架卡片”具有固定高度（85px），导致不同分类下的书签（或同一行中）高度不一致、参差不齐。 (已修复)
  - *改进点*：在 `style.css` 中为 `.card` 书签卡片、`.add-new-card` 新建卡片、`.skel-card` 骨架卡片统一显式声明 `height: var(--card-h, 105px)` 属性，实现自适应网格高度的 100% 绝对对齐。
- [x] **缺陷 6**：中央搜索框唤醒进行本地模糊检索时，由于匹配算法仅对书签标题（`title`）和描述（`desc`）进行了查询，导致用户输入网址、协议头或域名关键字（例如 `youtube`）时无法联想匹配站内卡片。 (已修复)
  - *改进点*：在 `app.js` 的模糊检索逻辑中，并列追加书签网址字段（`i.url`）小写模糊包含校验（`i.url.toLowerCase().includes(val)`），打通域名与直达书签的精准映射。
- [x] **缺陷 7**：手机端轻触放大镜呼出全屏搜索时，背景遮罩无法锁死滑动，且在轻触遮罩背景关闭搜索时发生“点击穿透”，误触底层的网址卡片并触发跳转。 (已修复)
  - *改进点*：在 `style.css` 中注入手势防穿透约束（`touch-action: none` / `touch-action: auto`），并在 `app.js` 中将关闭操作绑定至遮罩本身、在遮罩触控流中进行事件强制消费（`e.preventDefault(); e.stopPropagation();`），拦截一切向下泄露的手势，彻底阻断背景滚动与误触跳转。
- [x] **缺陷 8**：手机端在唤起完全体搜索框但没有输入文本时，底层的书签网格依然完全亮起（没有遮罩滤镜），且页面点击未做全局拦截，导致容易误点击底层网址卡片。 (已修复)
  - *改进点*：在 `style.css` 中将底层元素淡化（`opacity: 0.15`）与点击禁用（`pointer-events: none`）逻辑，以及全局背景模糊解脱机制（`backdrop-filter: none`）统一前置绑定到 `body.search-active`；同时在搜索激活时对非搜索层的 DOM 节点进行全局 `pointer-events: none !important` 强力屏蔽，确保唤出即锁定。
- [x] **缺陷 9**：全屏搜索框右侧的“X”（清空搜索）按钮无法正常工作，点击后输入框没有被清空。 (已修复)
  - *改进点*：在 `app.js` 的 `initSearch()` 中补齐了对 `#sea-clear-btn` 的点击事件处理逻辑：清空输入框、派发 `input` 自定义事件重置关联的检索面板状态、并重新将焦点定位回输入框，实现了顺畅的高效清除交互。

## 新增增强特性 (New Features)
- [x] **特性 1**：在卡片地址窗格上添加对外隐藏状态的小眼睛悬浮徽章，并在页面管理模式下升级为可交互的快捷切换按钮。
  - *改进点*：
    1. **普通浏览态下**：管理员/登录用户能清晰在卡片左上角看到 `ri-eye-off-line` 徽章，明确知道哪些已对外隐藏。
    2. **页面管理模式下**：在卡片右上角的管理员工具栏中，新增专属的可交互 `ri-eye-line`/`ri-eye-off-line` 按钮。用户可直接在管理视图下一键切换单个网址的显示/隐藏，无需点开编辑弹窗，修改会安全同步本地并利用已完善的退出自动机制完美刷新上云，操作效率提升 300%。
- [x] **特性 2**：进入页面管理模式后，所有卡片的管理按钮组（显隐小眼睛、编辑铅笔、删除垃圾桶）改为**全局常亮高能展示**。
  - *改进点*：免去了鼠标频繁悬浮划过的繁琐排查，一眼便知所有网址的显隐与配置状态。同时完美解决了**平板/手机等触摸屏设备**无法进行 “Hover（鼠标悬停）” 导致无法调出管理按钮的历史操作死角。
- [x] **特性 3**：优化管理模式下数据多频度上传机制，引入**“本地修改暂存 + 退出统一批处理上云”**的防网络拥堵合并策略。
  - *改进点*：将页面管理期间（如连续编辑书签、删除书签、移动或切换显隐）的高频单次上云 POST 请求全部解耦为本地轻量级暂存，避免频繁网络请求导致加载框反复阻断操作。在用户选择“保存并退出”时，一次性打包进行合并上云同步，让网络不佳的站长也能享受流畅丝滑的管理打理体验。
- [x] **特性 4**：移动端单手操作优化：将汉堡菜单和召唤搜索两个按钮统一整合到右侧，并在黄金触控区（中下部悬浮）垂直叠放。
  - *改进点*：在 `@media (max-width: 768px)` 移动端视口下，重置桌面端左侧布局：
    1. **搜索按钮**定位至右下角 `bottom: calc(140px + env(safe-area-inset-bottom, 0px))`，未激发态向右退缩 `translateX(20px)` 隐藏。
    2. **汉堡按钮**移动到右侧并垂直叠加于搜索按钮之上，定位至 `bottom: calc(192px + env(safe-area-inset-bottom, 0px))`，未激发态同样向右退缩 `translateX(20px)` 隐藏。
    3. **双重避让与美化**：不仅完美避开了顶部异形屏/刘海屏、底部常驻“回到顶部/回到低部”按钮，且在搜索状态开启时双按钮自动向上微移并优雅淡出，极大提升单手盲操与视觉对称美。
- [x] **特性 5**：页面管理模式（Page Management）体验重构与移动端闭环。
  - *改进点*：
    1. **搜索按钮全端自动隐藏**：在 `body.page-manage-active` 状态下，添加 `.summon-search-btn` 隐退动画，确保管理与编辑时不受搜索按钮干扰。
    2. **移动端支持点击外部关闭**：在手机端 `@media (max-width: 768px)` 媒体查询内，当侧边栏激活时恢复 `.sidebar-overlay` 显示，支持用户通过轻触侧边栏外区域顺畅收回侧边栏。
    3. **移动端汉堡按钮功能对齐**：当侧边栏打开时，将右置的 `.sidebar-toggle` 保持常亮显示。无论在普通态还是管理编辑态下，侧边栏展开后用户均可再次点击它直接关闭侧边栏，完美实现交互闭环。
    4. **锁定右下侧定位防止吸顶**：在移动端媒体查询下为按钮样式加入 `top: auto !important`，彻底阻断了由于处于管理员激活状态（`body.admin-mode`）或重要公告顶部栏存在而强行将按钮拉回顶部的历史响应式偏差，确保进入页面管理前、后按钮高度与位置 100% 绝对一致。
- [x] **特性 6**：打通并锁死禅意模式下的全屏搜索背景隔离与页面防滑动穿透。
  - *改进点*：
    1. **锁定层级穿透与页面滚动**：将 `overflow: hidden !important; height: 100% !important;` 滚动锁定不仅作用于 `body`，更双重锁定外层核心框架 `.app-layout`。彻底拦截移动端手势滑动并杜绝由于内容高度溢出带来的弹性滚动穿透。
    2. **强制穿透淡化禅意专属层**：在全屏搜索开启时，为专属导航视界（`.zen-horizon`）和特色书签容器（`.grid-container`）强制施加 `opacity: 0.15 !important` 和 `pointer-events: none !important`。强力瓦解禅意特色容器的高优先级，完美呈现与常规模式 100% 对齐的半透明暗色/亮色模糊遮罩隔离，且防止一切由于盲点底层卡片产生的误触与跳转。
    3. **浮动控制按钮全淡出**：搜索时对侧边栏收纳汉堡按钮、放大镜按钮、快速导航面板（`.quick-nav-group`）以及右下角禅意展开气泡（`.zen-expand-btn`）执行全面的 `opacity: 0 !important; pointer-events: none !important;` 高级淡出屏蔽，实现全端、全模式 100% 物理深度对齐。
- [x] **特性 7**：视觉实验室（Personalization Lab）弹窗高密度、轻量化重构。
  - *改进点*：
    1. **布局密度并列重构**：将布局密度按钮组（原“紧凑模式”、“标准平衡”、“极致透气”）精简重命名为“**紧凑**”、“**平衡**”、“**透气**”，并将其重构为与其他实验性选项完全统一的横向分段控制布局（`.segmented-control`），合并置于同一行，消除垂直堆叠。
    2. **说明文案高密度精简**：对弹窗内所有功能的提示与说明文字进行了全面瘦身压缩，大幅压榨垂直空间，配合选项组间距由 `15px` 调整为 `10px` 的微调，整个个性化面板高度缩减约 `120px`，极大提升了弹窗在移动端及小屏设备上的视觉完整度，实现“免滚动一屏展示”。
- [x] **特性 8**：打通移动端禅意模式下控制按钮的滚动退缩与动态弹出（与常规模式完美对齐）。
  - *改进点*：
    1. **闲置时向右贴边隐藏**：在手机端 `@media (max-width: 768px)` 媒体查询下，将禅意模式固定的 `transform: none !important; opacity: 1 !important` 强硬控制重构。默认未激活（闲置无操作）时，将汉堡按钮、搜索召唤按钮、回到顶部/回到底部按钮组全部对齐常规模式，赋予 `opacity: 0.45 !important` 和 `transform: translateX(20px) !important`，实现低打扰、更通透的呼吸感网页形态。
    2. **感应弹出机制**：新增禅意特异性触发联动选择器，当移动端触发 `scroll-active`（页面向下滚动）、`sidebar-open`（菜单展开）或直接 `hover`/`active` 触碰任意一个按钮时，强制满分全显（`opacity: 1 !important`）并平滑向左弹回原位（`transform: translateX(0) !important`），确保极简化美感与高效率操作完美并存。
- [x] **特性 9**：极致优化手机端四个控制按钮的贴边半隐藏与全隐退比例（常规与禅意双模式）。
  - *改进点*：根据手机端 `40px` 按钮和 `15px` 屏幕间隙，重塑了未激活态下的平移偏移比例：
    1. **汉堡与搜索按钮（退缩 35px，露出一半）**：将 translateX 调整为 `35px`。此时按钮右侧刚好越过屏幕边缘 `20px`，仅露出一半，既极富通透呼吸感，又方便随时轻触点按。
    2. **回到顶底按钮组（退缩 60px，完全隐藏）**：将 translateX 调整为 `60px`（溢出 15px+40px 宽度界限），使其在闲置时 100% 隐藏出镜，页面边缘绝对干净。

